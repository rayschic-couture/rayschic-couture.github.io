backend:
  name: git-gateway
  branch: main
  commit_messages:
    create: 'Création : {{collection}} - {{slug}}'
    update: 'Mise à jour : {{collection}} - {{slug}}'
    delete: 'Suppression : {{collection}} - {{slug}}'
    uploadMedia: 'Upload : {{path}}'
    deleteMedia: 'Suppression : {{path}}'

media_folder: "images"
public_folder: "/images"

collections:
  - name: "images"
    label: "🖼️ Images"
    folder: "images"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Nom", name: "name", widget: "string", required: true}
      - {label: "Image", name: "image", widget: "image", required: true}
      - {label: "Catégorie", name: "category", widget: "select", 
         options: ["hero", "costumes", "chemises", "pantalons", "vestes", "accessoires"]}
      - {label: "Description", name: "description", widget: "text"}

  - name: "costumes"
    label: "👔 Costumes"
    folder: "images/costumes"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Nom", name: "name", widget: "string", required: true}
      - {label: "Image", name: "image", widget: "image", required: true}
      - {label: "Description", name: "description", widget: "text"}

  - name: "chemises"
    label: "👕 Chemises"
    folder: "images/chemises"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Nom", name: "name", widget: "string", required: true}
      - {label: "Image", name: "image", widget: "image", required: true}
      - {label: "Description", name: "description", widget: "text"}
