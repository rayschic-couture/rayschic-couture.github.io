backend:
  name: git-gateway
  branch: main
  commit_messages:
    create: 'Ajout image : {{slug}}'
    update: 'Mise à jour image : {{slug}}'

media_folder: "images"
public_folder: "/images"

collections:
  - name: "costumes"
    label: "👔 Costumes"
    folder: "images/costumes"
    create: true
    slug: "{{name}}"
    fields:
      - {label: "Nom", name: "name", widget: "string"}
      - {label: "Image", name: "image", widget: "image"}

  - name: "chemises"
    label: "👕 Chemises"
    folder: "images/chemises"
    create: true
    slug: "{{name}}"
    fields:
      - {label: "Nom", name: "name", widget: "string"}
      - {label: "Image", name: "image", widget: "image"}

  - name: "pantalons"
    label: "👖 Pantalons"
    folder: "images/pantalons"
    create: true
    slug: "{{name}}"
    fields:
      - {label: "Nom", name: "name", widget: "string"}
      - {label: "Image", name: "image", widget: "image"}
